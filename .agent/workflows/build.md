---
description: Build the library and demo application
---

To build the entire project for production:

1. Build the library first:
```bash
npm run build:lib
```

2. Build the demo application:
```bash
npm run build:demo
```

Alternatively, use the combined script:
```bash
npm run build:all
```
